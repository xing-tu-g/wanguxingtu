# assets 目录规则

- 这里放真实可用的美术资源，优先使用真实 Alpha，不要用假透明边蒙混。
- 按钮资源要保留 normal、hover、pressed 三态的一致性。
- `icon_settings_default.png` 和 `icon_settings_hover.png` 继续标记为 `needs_art_fix`，直到正式修复完成。
- 不要为了省事替换成会误导后续工作的占位资源。
